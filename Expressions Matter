# Given three integers a ,b ,c, return the largest number obtained after inserting the following operators and brackets: +, *, ()

SELECT 
GREATEST(a + b + c, a * b * c, a * (b + c), (a + b) * c) 
AS res
FROM expression_matter;
